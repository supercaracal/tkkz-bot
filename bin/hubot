#!/bin/bash

set -e

cd $(dirname $0)/..

npm install -q -y

export PATH="node_modules/.bin:node_modules/hubot/node_modules/.bin:$PATH"

npm run-script build

exec node_modules/.bin/hubot --name "tkkz" "$@"
